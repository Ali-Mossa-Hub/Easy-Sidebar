/// The constant app durations
///
class DefaultDurations {
  ///
  static const defaultSidebarDuration = Duration(milliseconds: 350);

  ///
  static const defaultSidebarItemDuration = Duration(milliseconds: 350);

  ///
  static const defaultMainPageDuration = Duration(milliseconds: 350);

  ///
  static const defaultMaterialWidgetDuratoin = Duration(milliseconds: 50);
}
